<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/K_tool.tab/MISC.panel/MISC1.stack/Grid_DIM.pushbutton/dimGrid_script.py">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/K_tool.tab/MISC.panel/MISC1.stack/Grid_DIM.pushbutton/dimGrid_script.py" />
              <option name="originalContent" value="&quot;&quot;&quot;e&quot;&quot;&quot;&#10;__title__ = 'dim Grids'&#10;__author__ = 'LaK'&#10;&#10;# Import&#10;import clr&#10;import Autodesk.Revit.DB as DB&#10;&#10;&#10;# Function to check gradient&#10;def gradient(grid):&#10;    gr = None&#10;    start = grid.Curve.GetEndPoint(0)&#10;    end = grid.Curve.GetEndPoint(1)&#10;    if round(start.X, 10) != round(end.X, 10):&#10;        gr = round((1.0 / (start.X - end.X)) * (start.Y - end.Y), 10)&#10;    return gr&#10;&#10;&#10;# Function to check if lines are parallel&#10;def parallel(gridA, gridB):&#10;    return gradient(gridA) == gradient(gridB)&#10;&#10;&#10;# Function to create ReferenceArray&#10;def refArray(listConv):&#10;    refArray = DB.ReferenceArray()&#10;    for e in listConv:&#10;        refArray.Append(DB.Reference(e))&#10;    return refArray&#10;&#10;&#10;# Function to create reference line for dimension&#10;def refLine(grids):&#10;    start = grids[0].Curve.GetEndPoint(0)&#10;    end = grids[1].Curve.GetEndPoint(0)&#10;    line = DB.Line.CreateBound(start, end)&#10;    return line&#10;&#10;&#10;# Get current view&#10;view = doc.ActiveView&#10;&#10;# Select all grids by filter&#10;gridsFilter = DB.ElementCategoryFilter(DB.BuiltInCategory.OST_Grids)&#10;gridsCollector = DB.FilteredElementCollector(doc).WherePasses(gridsFilter).WhereElementIsNotElementType()&#10;&#10;# Convert gridsCollector into list and split them into parallel groups&#10;grids = list(gridsCollector)&#10;gridGroups = {}&#10;excludedGrids = []&#10;# Loop through all grids&#10;for grid in grids:&#10;    gridName = grid.LookupParameter(&quot;Name&quot;).AsString()&#10;    gridCurve = grid.Curve&#10;    # Check if grid is already classified&#10;    if gridName not in excludedGrids:&#10;        # Check if the rest of the grids are parallel&#10;        for g in grids:&#10;            inter = gridCurve.Intersect(g.Curve)&#10;            gName = g.LookupParameter(&quot;Name&quot;).AsString()&#10;            # Check parallel grids and group them&#10;            if gName not in excludedGrids and inter == DB.SetComparisonResult.Disjoint and parallel(grid, g):&#10;                if gridName not in gridGroups.keys():&#10;                    gridGroups[gridName] = [grid]&#10;                    excludedGrids.append(gridName)&#10;                gridGroups[gridName].append(g)&#10;                excludedGrids.append(gName)&#10;&#10;# Create transaction to create dimensions&#10;t = DB.Transaction(doc, &quot;Dimension grids&quot;)&#10;t.Start()&#10;&#10;for k in gridGroups.keys():&#10;    lt = gridGroups[k]&#10;    line = refLine(lt)&#10;    ref = refArray(lt)&#10;    try:&#10;        # Create dimensions&#10;        doc.Create.NewDimension(view, line, ref)&#10;    except:&#10;        pass&#10;&#10;# Commit transaction&#10;t.Commit()" />
              <option name="updatedContent" value="&quot;&quot;&quot;e&quot;&quot;&quot;&#10;__title__ = 'dim Grids'&#10;__author__ = 'LaK'&#10;&#10;# Import&#10;import clr&#10;import Autodesk.Revit.DB as DB&#10;&#10;&#10;# Ensure 'doc' and 'uidoc' are defined (pyRevit / RevitPythonShell / Dynamo compatibility)&#10;try:&#10;    doc  # noqa: F821&#10;except NameError:&#10;    uidoc = None&#10;    doc = None&#10;    # 1) Try pyRevit&#10;    try:&#10;        uidoc = __revit__.ActiveUIDocument  # type: ignore[name-defined]&#10;        doc = uidoc.Document  # type: ignore[name-defined]&#10;    except Exception:&#10;        pass&#10;&#10;    # 2) Try RevitServices (RevitPythonShell / Dynamo)&#10;    if doc is None:&#10;        try:&#10;            from RevitServices.Persistence import DocumentManager  # type: ignore&#10;            dm = DocumentManager.Instance&#10;            doc = dm.CurrentDBDocument&#10;            # CurrentUIApplication may not exist in some hosts; guard it&#10;            try:&#10;                uidoc = dm.CurrentUIApplication.ActiveUIDocument&#10;            except Exception:&#10;                uidoc = None&#10;        except Exception:&#10;            pass&#10;&#10;    # 3) If still not found, fail with clear message&#10;    if doc is None:&#10;        raise RuntimeError(&#10;            &quot;The Revit Document 'doc' is not available. &quot;&#10;            &quot;Run this script inside pyRevit, RevitPythonShell, or another Revit-hosted Python environment &quot;&#10;            &quot;that exposes the active Document (e.g., provide 'doc' and 'uidoc' before running).&quot;&#10;        )&#10;&#10;&#10;# Function to check gradient&#10;def gradient(grid):&#10;    gr = None&#10;    start = grid.Curve.GetEndPoint(0)&#10;    end = grid.Curve.GetEndPoint(1)&#10;    if round(start.X, 10) != round(end.X, 10):&#10;        gr = round((1.0 / (start.X - end.X)) * (start.Y - end.Y), 10)&#10;    return gr&#10;&#10;&#10;# Function to check if lines are parallel&#10;def parallel(gridA, gridB):&#10;    return gradient(gridA) == gradient(gridB)&#10;&#10;&#10;# Function to create ReferenceArray&#10;def refArray(listConv):&#10;    refArray = DB.ReferenceArray()&#10;    for e in listConv:&#10;        refArray.Append(DB.Reference(e))&#10;    return refArray&#10;&#10;&#10;# Function to create reference line for dimension&#10;def refLine(grids):&#10;    start = grids[0].Curve.GetEndPoint(0)&#10;    end = grids[1].Curve.GetEndPoint(0)&#10;    line = DB.Line.CreateBound(start, end)&#10;    return line&#10;&#10;&#10;# Get current view&#10;view = doc.ActiveView&#10;&#10;# Select all grids by filter&#10;gridsFilter = DB.ElementCategoryFilter(DB.BuiltInCategory.OST_Grids)&#10;gridsCollector = DB.FilteredElementCollector(doc).WherePasses(gridsFilter).WhereElementIsNotElementType()&#10;&#10;# Convert gridsCollector into list and split them into parallel groups&#10;grids = list(gridsCollector)&#10;gridGroups = {}&#10;excludedGrids = []&#10;# Loop through all grids&#10;for grid in grids:&#10;    gridName = grid.LookupParameter(&quot;Name&quot;).AsString()&#10;    gridCurve = grid.Curve&#10;    # Check if grid is already classified&#10;    if gridName not in excludedGrids:&#10;        # Check if the rest of the grids are parallel&#10;        for g in grids:&#10;            inter = gridCurve.Intersect(g.Curve)&#10;            gName = g.LookupParameter(&quot;Name&quot;).AsString()&#10;            # Check parallel grids and group them&#10;            if gName not in excludedGrids and inter == DB.SetComparisonResult.Disjoint and parallel(grid, g):&#10;                if gridName not in gridGroups.keys():&#10;                    gridGroups[gridName] = [grid]&#10;                    excludedGrids.append(gridName)&#10;                gridGroups[gridName].append(g)&#10;                excludedGrids.append(gName)&#10;&#10;# Create transaction to create dimensions&#10;t = DB.Transaction(doc, &quot;Dimension grids&quot;)&#10;t.Start()&#10;&#10;for k in gridGroups.keys():&#10;    lt = gridGroups[k]&#10;    line = refLine(lt)&#10;    ref = refArray(lt)&#10;    try:&#10;        # Create dimensions&#10;        doc.Create.NewDimension(view, line, ref)&#10;    except:&#10;        pass&#10;&#10;# Commit transaction&#10;t.Commit()" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/RevitAddin/WorksetSelectorWindow.xaml.cs">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/RevitAddin/WorksetSelectorWindow.xaml.cs" />
              <option name="updatedContent" value="using System;&#10;using System.Collections.Generic;&#10;using System.Linq;&#10;using System.Windows;&#10;&#10;namespace KTool.Revit&#10;{&#10;    public partial class WorksetSelectorWindow : Window&#10;    {&#10;        public List&lt;string&gt; SelectedWorksets { get; private set; }&#10;&#10;        public WorksetSelectorWindow(IList&lt;string&gt; worksets)&#10;        {&#10;            InitializeComponent();&#10;            // ...existing code...&#10;            WorksetListBox.ItemsSource = worksets;&#10;        }&#10;&#10;        private void Ok_Click(object sender, RoutedEventArgs e)&#10;        {&#10;            SelectedWorksets = WorksetListBox.SelectedItems.Cast&lt;string&gt;().ToList();&#10;            DialogResult = true;&#10;            Close();&#10;        }&#10;&#10;        private void Cancel_Click(object sender, RoutedEventArgs e)&#10;        {&#10;            DialogResult = false;&#10;            Close();&#10;        }&#10;    }&#10;}&#10;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>