<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User Input Form" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Select Partition (row 0) -->
        <TextBlock Grid.Row="0" Text="Select Partition:" Margin="0,0,0,5"/>
        <ComboBox x:Name="Select_partition" Grid.Row="0" Margin="0,22,0,0"/>

        <!-- Select rebar number (row 1) - ComboBox non-editable -->
        <TextBlock Grid.Row="1" Text="Select rebar number:" Margin="0,10,0,5"/>
        <ComboBox x:Name="Select_rebar_number" Grid.Row="1" Margin="0,32,0,0" IsEditable="False"/>

        <!-- New rebar number (row 2) -->
        <TextBlock Grid.Row="2" Text="New rebar number:" Margin="0,10,0,5"/>
        <TextBox x:Name="New_Rebar_number" Grid.Row="2" Margin="0,32,0,0" Text="10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="cancel_button" Content="Cancel" Width="80" Margin="0,0,10,0"/>
            <Button x:Name="ok_button" Content="OK" Width="80"/>
        </StackPanel>
    </Grid>
</Window>